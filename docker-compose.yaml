services:
  catalyst-center-mcp:
    build: .
    ports:
      - "7001:7001"
    environment:
      CATALYST_CENTER_HOST: ${CATALYST_CENTER_HOST}
      CATALYST_CENTER_USERNAME: ${CATALYST_CENTER_USERNAME}
      CATALYST_CENTER_PASSWORD: ${CATALYST_CENTER_PASSWORD}
      CATALYST_CENTER_VERIFY_SSL: ${CATALYST_CENTER_VERIFY_SSL:-false}
