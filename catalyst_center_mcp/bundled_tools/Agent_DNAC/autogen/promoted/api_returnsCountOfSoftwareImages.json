{
  "function": {
    "_meta": {
      "agents": [
        "catalyst_network_engineer_agent"
      ],
      "notes": [
        "Pass productNameOrdinal as a number.",
        "Keep the standard/golden mapping implicit unless the user explicitly asks.",
        "This tool has no recommended filter."
      ]
    },
    "additionalMetadata": {
      "method": "GET",
      "parameterLocation": {
        "golden": "query",
        "hasAddonImages": "query",
        "imported": "query",
        "integrity": "query",
        "isAddonImages": "query",
        "name": "query",
        "productNameOrdinal": "query",
        "siteId": "query",
        "supervisorProductNameOrdinal": "query",
        "version": "query"
      },
      "uri": "/dna/intent/api/v1/images/count",
      "version": "2.3.7.9"
    },
    "description": "Returns the software-image count for a site.\n\nRequirements:\n- Resolve `siteId` via `api_getSites` unless the user already gave a valid UUID. Use `name=\"Global\"` for the global site.\n- State what the count covers: imported images, Cisco.com latest/recommended images, or both, depending on the filters used.\n- This tool has no recommended filter and does not return recommendation status. Do not use `golden=true` to answer recommended-image count requests.\n- Preserve the user's term: `standard`, `golden`, or `recommended`. If the user asks for `standard`, never say `golden` or add aliases like `standard (golden)`.\n- Treat `golden` and the `golden` query parameter as internal details unless the user explicitly asks about that relationship.",
    "name": "api_returnsCountOfSoftwareImages",
    "parameters": {
      "properties": {
        "golden": {
          "description": "Internal filter for standard/golden image count requests. Do not expose this mapping in user-facing text unless the user explicitly asks. Do not use it for recommended-image counts unless the user also asked for standard or golden.",
          "type": "string"
        },
        "hasAddonImages": {
          "description": "When set to\u00a0`true`, it will retrieve the images which have add-on images. When set to\u00a0`false`, it will retrieve the images which do not have add-on images.",
          "type": "boolean"
        },
        "imported": {
          "description": "When the value is set to `true`, it will include physically imported images. Conversely, when the value is set to `false`, it will include image records from the cloud. The identifier for cloud images can be utilised to download images from Cisco.com to the disk.",
          "type": "boolean"
        },
        "integrity": {
          "description": "Filter with verified images using Integrity Verification Available values: UNKNOWN, VERIFIED",
          "type": "string"
        },
        "isAddonImages": {
          "description": "When set to `true`, it will retrieve the images that an add-on image.  When set to `false`, it will retrieve the images that are not add-on images",
          "type": "boolean"
        },
        "name": {
          "description": "Software image or add-on image filename, not UUID. Accept exact values or regex/wildcard patterns such as `.*cat9k.*`, `cat.*`, or `.*\\.bin`. Minimum 3 characters.",
          "type": "string"
        },
        "productNameOrdinal": {
          "description": "Numeric product ordinal from `api_returnsNetworkDeviceProductNamesForASite`.",
          "type": "number"
        },
        "siteId": {
          "description": "Site UUID. Resolve site names with `api_getSites` first, including `name=\"Global\"` for the global site.",
          "type": "string"
        },
        "supervisorProductNameOrdinal": {
          "description": "The supervisor engine module ordinal is a unique value for each supervisor module. The `supervisorProductNameOrdinal` can be obtained from the response of API `/dna/intent/api/v1/siteWiseProductNames`",
          "type": "number"
        },
        "version": {
          "description": "Image version. Accept exact values or regex/wildcard patterns such as `16.*` or `.*\\.18.*`. Minimum 3 characters.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "responses": {
      "description": "",
      "properties": {
        "response": {
          "description": "Response",
          "properties": {
            "count": {
              "description": "Reports a count, for example, a total count of records in a given resource.",
              "type": "integer"
            }
          },
          "type": "object"
        },
        "version": {
          "description": "The version of the response",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  "type": "function"
}
